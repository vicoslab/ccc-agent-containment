# bash completion for ccc-agent. Installed by pip into the shell's standard
# bash-completion directory.
_ccc_agent_complete()
{
    local IFS=$'\n'
    COMPREPLY=( $(ccc-agent __complete bash "$COMP_CWORD" "${COMP_WORDS[@]}" 2>/dev/null) )
}
complete -o default -F _ccc_agent_complete ccc-agent
