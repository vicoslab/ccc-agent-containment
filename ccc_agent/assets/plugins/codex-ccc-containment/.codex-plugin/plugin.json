{
  "name": "ccc-agent",
  "version": "0.1.0",
  "description": "CCC contained-session lifecycle hooks for Codex. Injected read-only into the bwrap sandbox by ccc-agent run for contained Codex sessions only; never loaded by a direct `codex` run. The blocking Stop hook signals turn boundaries to the trusted supervisor over the control socket; it never holds commit authority.",
  "author": {
    "name": "CCC agent"
  }
}
