{
  "name": "ccc-agent",
  "version": "0.1.0",
  "description": "CCC contained-session lifecycle hooks. Injected read-only into the bwrap sandbox by ccc-agent run for contained Claude sessions only; never loaded by a direct `claude` run. Signals turn boundaries to the trusted supervisor over the control socket; it never holds commit authority.",
  "author": {
    "name": "CCC agent"
  }
}
